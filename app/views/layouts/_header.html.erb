<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="pull-left logo" href="<%= root_path %>"><%= image_tag "logo.png", alt: "logo", class: "img-responsive brand-image" %></a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="active"><a href="<%= root_path %>">Home</a></li>
        <% if current_user && (current_user.has_role? :admin) %>
          <li><a href="<%= admin_users_path %>">users</a></li>
          <li><a href="<%= admin_roles_path %>">roles</a></li>
          <li><a href="<%= admin_groups_path %>">groups</a></li>
          <li><a href="<%= admin_users_roles_path %>">User roles</a></li>
          <li><a href="<%= admin_document_categories_path %>">document category</a></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= search_form_for @q, url: search_documents_path, html: { method: :post, role: "form"} do |f| %>
            <%= f.search_field :name_cont, placeholder: "document search", class: "form-control" %>
            <%= f.submit %>
          <% end %>
        </li>



        <% if user_signed_in? %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><a href="/users/sign_up"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
          <li><a href="/login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
  <hr style="border-top: 2px solid green; margin-top:-1.4%;">
