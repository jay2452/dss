$('#uploadUser-Logs').append('<%= escape_javascript render(@uploadUserLogs) %>');
<% if @uploadUserLogs.next_page %>
  $('.pagination').replaceWith('<%= escape_javascript will_paginate(@uploadUserLogs) %>');
  // $('.pagination').hide();
<% else %>
  $('#gif-loader').hide();
<% end %>
