$('#documents').append('<%= escape_javascript render(@documents) %>');
<% if @documents.next_page %>
  $('.pagination').replaceWith('<%= escape_javascript will_paginate(@documents) %>');
  // $('.pagination').hide();
<% else %>
  $('#gif-loader').hide();
<% end %>
