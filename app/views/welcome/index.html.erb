<% if current_user.has_role? :admin %>
    <%# goto admin adsh board %>
    <% provide :title, "Welcome - Admin" %>
  <%= render :partial => "admin_dash_board" %>
<% else %>
  <% provide :title, "Welcome" %>
  <%# render template: "documents/index" %>


    <div class="container-fluid">
      <% if current_user #&& (can? :create, @document) %>
          <%= link_to 'Upload Document', new_document_path %>
      <% end %>

      <div class="col-md-3">
        <div class="panel panel-default">
          <div class="panel-heading">Groups Associated</div>
          <div class="panel-body">
            <% @user_groups.each do |group| %>
              <%= group.name %> <br>
            <% end %>
          </div>
        </div>
      </div>

      <div class="col-md-6">


        <div class="panel panel-default">
          <div class="panel-heading">
            Recent Documents <!-- <small>(both approved and unapproved)</small> -->
            <%# documents will be shown according to the groups in which file was uploaded %>
          </div>
            <% @recent_docs.each do |doc| %>
              <div class="panel-body">
                <%= link_to doc.name, document_path(doc) %> uploaded : <small><%= time_ago_in_words(doc.created_at) %> ago
                </small> uploaded by: <small><%= doc.user.email %></small> <br>
              </div>
            <% end %>
        </div>
      </div>

      <div class="col-md-3">

      </div>
    </div>

    <!-- <table class="table table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th>category</th>
          <th>uploaded by</th>
          <th>download</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @documents.each do |document| %>
          <tr>
            <td><%= document.name %></td>
            <td><%= document.document_category.name %></td>
            <td><%= document.user.email %></td>
            <td><%= link_to "view file", document.file.url, target: '_blank' if document.file? %></td>
            <td><%= link_to 'Show', document %></td>
            <td><%= link_to 'Edit', edit_document_path(document) %></td>
            <td><%= link_to 'Destroy', document, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table> -->

<% end %>
