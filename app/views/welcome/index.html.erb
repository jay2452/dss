<% if (current_user.has_role? :admin) || (current_user.has_role? :superAdmin) %>
    <%# goto admin adsh board %>
    <% provide :title, "Welcome - Admin" %>

  <!-- <nav class="navbar navbar-default">
    <div class="container-fluid">
      <ul class="nav navbar-nav">
        <li><a href="#">Section 1</a></li>
      </ul>
    </div>

  </nav> -->

  <%= render :partial => "admin_dash_board" %>

<% elsif current_user.has_role? :uploadUser %>

  <%= render template: "documents/index" %>

<% elsif current_user.has_role? :viewUser %>
  <% provide :title, "Welcome" %>
    <div class="container-fluid">
      <div class="col-md-2"></div>
      <div class="col-md-8">
        <% if @recent_docs.empty? %>
          <div class="jumbotron">
            <h3 align="center"> No Documents</h3>
          </div>
        <% else %>
          <h4 align="center"> Recent Documents</h4>
          <div class="recent-docs">
            <%= render @recent_docs %>
          </div>
          <%= will_paginate @recent_docs %>
        <% end %>


      </div>

      <div class="col-md-2"></div>
    </div>
<% end %>
